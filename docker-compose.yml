version: '3'
services: 
  fun5-mongo:
    image: mongo
    container_name: fun5-mongo
    volumes:
      - 'docker-volumes/mongo/data:/data/db'
    ports:
      - 27017:27017
  fun5-b-backend:
    depend_on:
      - fun5-mongo
    build:
      context: .
      dockerfile: Dockerfile
    image: fun5-b-backend
    container_name: fun5-b-backend
    ports:
      - 7001:7001

